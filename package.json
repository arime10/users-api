{
  "name": "users-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT",
    "postinstall": "npm install json-server"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
